#!/bin/sh
set -euxo pipefail
wget -qO- 'https://tailscale.com/install.sh' | sh
sleep 5
rc-service tailscale -S start || systemctl enable --now tailscaled.service
tailscale up --auth-key='${tskey}' --ssh
