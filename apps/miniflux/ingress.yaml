---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
      - miniflux
  defaultBackend:
    service:
      name: miniflux-app
      port:
        number: 80
 
